# Autom8r backend — pinned to current stable majors (verified Sept 2026).
# Install: pip install -r requirements.txt
fastapi>=0.140,<1
uvicorn[standard]>=0.49,<1
pydantic>=2.13,<3
pydantic-settings>=2.14,<3
sqlalchemy>=2.0.51,<3
openai>=3.14,<4
mcp>=2.0,<3
httpx>=0.28,<1          # required by FastAPI TestClient
pytest>=9.1,<10
pytest-asyncio>=1.4,<2

# --- Optional: Google Sheets adapter (spec section 23) ---
# Only needed when GOOGLE_SHEETS_ENABLED=true. Imported lazily so the
# base install never requires Google libraries.
# gspread>=6.2,<7
# google-auth>=2.40,<3
